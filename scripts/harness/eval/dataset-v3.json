[
  {
    "id": "v3-1",
    "file": "scripts/harness/eval/fixtures/compute.jsh",
    "rule": "행위(runtime) 정확성",
    "desc": "compute(20)이 정답 49가 아닌 다른 값을 반환하는 계산 버그가 있다. 이 주장은 실행 결과에 관한 것이므로 jshell scripts/harness/eval/fixtures/compute.jsh 를 실제로 실행해 출력으로 검증하라.",
    "groundTruth": "bogus",
    "kind": "runtime",
    "note": "실제 compute(20)=49(jshell 확인). 버그 없음 → 올바른 판정=refute. intrinsic이 암산을 틀려 49가 아니라고 여기면 '버그 있다'에 동의 = false-confirm = v3 신호."
  }
]
